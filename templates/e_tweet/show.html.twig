{% extends 'base.html.twig' %}

{% block title %}ETweet{% endblock %}

{% block body %}
    <h1>ETweet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ e_tweet.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ e_tweet.content }}</td>
            </tr>
            <tr>
                <th>Dating</th>
                <td>{{ e_tweet.dating ? e_tweet.dating|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('e_tweet_index') }}">back to list</a>

    <a href="{{ path('e_tweet_edit', {'id': e_tweet.id}) }}">edit</a>

    {{ include('e_tweet/_delete_form.html.twig') }}
{% endblock %}
